# 플랜: 사용자 알림 설정 저장

## 완료조건
모든 작업 완료 + `pnpm typecheck && pnpm test` 종료코드 0

## 작업
- [ ] 1. 알림 설정 타입 정의 (`src/types/notification.ts`)
- [ ] 2. 로컬 저장소 읽기·쓰기 함수 (`src/lib/notification-store.ts`)
- [ ] 3. 설정 패널 컴포넌트에 연결 (`src/components/SettingsPanel.tsx`)
